const resultado = document.getElementById("resultado")
const alerta = confirm("você que receber novidades")

resultado.innerHTML = alerta
if (alerta){
    "então ta bom"
}
else {
    "então não mandeirei nada"
}